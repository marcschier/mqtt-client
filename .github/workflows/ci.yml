name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-test:
    name: build-test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    env:
      DOTNET_NOLOGO: 'true'
      DOTNET_CLI_TELEMETRY_OPTOUT: 'true'
      CI: 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            9.0.x
            10.0.x

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release --no-restore /warnaserror

      - name: Test (unit + api)
        shell: bash
        run: |
          ext="${{ runner.os == 'Windows' && '.exe' || '' }}"
          for proj in Mqtt.Client.UnitTests Mqtt.Client.ApiTests; do
            dotnet build "tests/$proj/$proj.csproj" -c Release --no-restore
            for tfm in net8.0 net9.0 net10.0; do
              echo "=== Running $proj on $tfm ==="
              "./tests/$proj/bin/Release/$tfm/$proj$ext" --no-ansi --no-progress
            done
          done

  aot:
    name: native-aot (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    env:
      DOTNET_NOLOGO: 'true'
      DOTNET_CLI_TELEMETRY_OPTOUT: 'true'
      CI: 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            9.0.x
            10.0.x

      - name: Publish unit tests (NativeAOT)
        run: dotnet publish tests/Mqtt.Client.UnitTests/Mqtt.Client.UnitTests.csproj -c Release -f net10.0 /warnaserror

      - name: Run AOT unit tests
        shell: bash
        run: |
          bin=$(find tests/Mqtt.Client.UnitTests/bin/Release/net10.0 -path '*/publish/*' -type f -name 'Mqtt.Client.UnitTests*' ! -name '*.pdb' ! -name '*.dll' ! -name '*.json' | head -n1)
          echo "Running $bin"
          "$bin" --no-ansi --no-progress

  pack:
    name: pack-nupkg
    runs-on: ubuntu-latest
    needs: [build-test, aot]
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet pack src/Mqtt.Client/Mqtt.Client.csproj -c Release -o ./nupkg
      - uses: actions/upload-artifact@v4
        with:
          name: nupkg
          path: ./nupkg/*.*nupkg
